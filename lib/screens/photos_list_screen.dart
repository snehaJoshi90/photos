import 'package:flutter/material.dart';

class PhotosListScreen extends StatelessWidget {
  const PhotosListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:const Text('Photos list'),),
    );
  }
}
